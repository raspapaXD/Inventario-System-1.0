// src/components/AppMenu.jsx

/*
 * La navegación principal ahora vive en AppLayout + AppSidebar.
 *
 * Dejamos este componente temporalmente para no tener que
 * modificar todos los imports de las páginas de una sola vez.
 * No renderiza ningún botón.
 */

export default function AppMenu() {
  return null;
}
